<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Chat Website</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f2f2f2;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      height: 100vh;
    }
    header {
      background: #4CAF50;
      color: white;
      text-align: center;
      padding: 15px;
      font-size: 20px;
    }
    #chat {
      flex: 1;
      padding: 15px;
      overflow-y: auto;
      background: white;
      border-top: 2px solid #ccc;
      border-bottom: 2px solid #ccc;
    }
    .message {
      margin: 8px 0;
      padding: 8px 12px;
      border-radius: 12px;
      max-width: 70%;
      word-wrap: break-word;
    }
    .you {
      background: #4CAF50;
      color: white;
      margin-left: auto;
      text-align: right;
    }
    .other {
      background: #ddd;
      color: black;
      margin-right: auto;
    }
    #form {
      display: flex;
      padding: 10px;
      background: #eee;
    }
    #input {
      flex: 1;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 8px;
    }
    button {
      margin-left: 10px;
      padding: 10px 15px;
      border: none;
      border-radius: 8px;
      background: #4CAF50;
      color: white;
      cursor: pointer;
    }
    button:hover {
      background: #45a049;
    }
  </style>
</head>
<body>
  <header>🌐 My Chat Website</header>

  <div id="chat"></div>

  <div id="form">
    <input id="input" type="text" placeholder="Type a message..." autocomplete="off" />
    <button onclick="sendMessage()">Send</button>
  </div>

  <script>
    // --- Name setup with filtering ---
    const bannedWords = ["badword1","badword2","badword3"]; // expand this list
    let username = localStorage.getItem("chatName");

    function setName() {
      let name = prompt("Enter your chat name:");
      if (!name) name = "Guest";
      let lower = name.toLowerCase();
      if (bannedWords.some(word => lower.includes(word))) {
        alert("That name isn’t allowed. Using Guest instead.");
        name = "Guest";
      }
      username = name;
      localStorage.setItem("chatName", name);
    }

    if (!username) setName();

    // --- Chat logic (local only for now) ---
    const chat = document.getElementById("chat");
    const input = document.getElementById("input");

    function sendMessage() {
      const text = input.value.trim();
      if (!text) return;
      
      // Show message
      addMessage(username, text, true);
      
      input.value = "";
    }

    function addMessage(sender, text, isYou=false) {
      const div = document.createElement("div");
      div.classList.add("message");
      div.classList.add(isYou ? "you" : "other");
      div.textContent = (isYou ? "You: " : sender + ": ") + text;
      chat.appendChild(div);
      chat.scrollTop = chat.scrollHeight;
    }

    // --- Allow Enter key ---
    input.addEventListener("keypress", function(e) {
      if (e.key === "Enter") {
        sendMessage();
      }
    });

    // --- Add a button to change name ---
    const nameBtn = document.createElement("button");
    nameBtn.textContent = "Set Name";
    nameBtn.style.marginLeft = "10px";
    nameBtn.onclick = setName;
    document.getElementById("form").appendChild(nameBtn);
  </script>
</body>
</html>
